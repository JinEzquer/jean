{% extends 'base.html.twig' %}


{% block stylesheets %}
  {{ parent() }}
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Arimo:wght@400;600;700&family=Bebas+Neue&display=swap" rel="stylesheet">
  <style>
    /* Reset default margins and padding */
    html, body {
      margin: 0;
      padding: 0;
      height: 100%;
      overflow-y: auto;
      overflow-x: hidden;
      font-family: 'Arimo', system-ui, -apple-system, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif;
    }
    /* Ensure all text uses Arimo font */
    * {
      font-family: 'Arimo', system-ui, -apple-system, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif;
    }
    
    /* Hide the main navigation bar and footer in staff section */
    .navbar, footer {
      display: none !important;
    }
    
    /* Adjust the main content to account for removed navbar */
    .staff-main {
      margin-left: 240px !important;
      padding: 0 !important;
      width: calc(100% - 240px) !important;
      min-height: 100vh;
    }
    body {
      margin: 0;
      padding: 0;
    }
    .staff-shell {
      display: flex;
      min-height: 100vh;
      margin: 0;
      padding: 0;
      position: relative;
    }
    .staff-aside {
      width: 240px;
      color: #e5eff7;
      padding: 0 12px 16px 12px;
      display: flex;
      flex-direction: column;
      gap: 0;
      margin: 0;
      position: fixed;
      top: 0;
      left: 0;
      bottom: 0;
      overflow-y: auto;
      scrollbar-width: none; /* Firefox */
      -ms-overflow-style: none; /* IE and Edge */
      background:
        /* subtle moving glow overlay */
        radial-gradient(1200px 600px at -20% -10%, rgba(30,64,90,.20), transparent 60%),
        radial-gradient(800px 400px at 120% 110%, rgba(2,132,199,.07), transparent 60%),
        /* low-poly base */
        url("data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='480' height='1600' viewBox='0 0 480 1600'><rect width='480' height='1600' fill='%230b1f2d'/><polygon points='0,0 240,0 120,160' fill='%23111f2f' /><polygon points='240,0 480,0 360,160' fill='%23122a3a' /><polygon points='120,160 240,0 360,160 240,320' fill='%23102b3e' /><polygon points='0,320 120,160 240,320 120,480' fill='%230e2333' /><polygon points='240,320 360,160 480,320 360,480' fill='%230e2a3b' /><polygon points='120,480 240,320 360,480 240,640' fill='%23102a3a' /><polygon points='0,640 120,480 240,640 120,800' fill='%230d2130' /><polygon points='240,640 360,480 480,640 360,800' fill='%230f2a3b' /><polygon points='120,800 240,640 360,800 240,960' fill='%23112f43' /><polygon points='0,960 120,800 240,960 120,1120' fill='%230e2333' /><polygon points='240,960 360,800 480,960 360,1120' fill='%23102c40' /></svg>") center/cover no-repeat;
      background-size: 200% 200%, 200% 200%, cover;
      animation: azureFlow 14s ease-in-out infinite;
    }
    @keyframes azureFlow{0%{background-position:0% 50%, 100% 50%, 0% 50%}50%{background-position:100% 50%, 0% 50%, 100% 50%}100%{background-position:0% 50%, 100% 50%, 0% 50%}}
    .staff-brand {
      font-weight: 900;
      color: #fff;
      margin: 0;
      padding: 16px 0;
      font-size: 1.5rem;
      line-height: 1.2;
      position: relative;
      z-index: 1;
    }
    
    .staff-brand .logo {
      display: flex;
      align-items: center;
      gap: 10px;
      text-decoration: none;
      color: inherit;
    }
    
    .staff-brand .logo img {
      width: 40px;
      height: 40px;
      object-fit: contain;
      flex-shrink: 0;
    }
    .profile {
      display: flex;
      align-items: center;
      gap: 12px;
      padding: 12px;
      border-radius: 12px;
      background: rgba(255,255,255,.05);
      margin-top: 10px;
    }
    .avatar {
      width: 60px;
      height: 60px;
      border-radius: 50%;
      overflow: hidden;
      background: #e6f3ff;
      display: flex;
      align-items: center;
      justify-content: center;
      flex-shrink: 0;
    }
    .avatar img{width:100%;height:100%;object-fit:cover;display:block}
    .meta{display:flex;flex-direction:column;line-height:1.1}
    .meta .name{font-weight:800;color:#fff}
    .status{display:flex;align-items:center;gap:6px;font-weight:700;color:#86efac}
    .status .dot{width:10px;height:10px;border-radius:50%;background:#22c55e;animation:blink 1.2s steps(1,end) infinite}
    @keyframes blink{0%{opacity:1}50%{opacity:.1}100%{opacity:1}}
    .status.offline{color:#cbd5e1}
    .status.offline .dot{background:#94a3b8;box-shadow:none;animation:none}
    .staff-nav a{display:flex;align-items:center;gap:10px;color:#cbd5e1;text-decoration:none;padding:8px 10px;border-radius:8px;font-size:16px}
    .staff-nav a:hover{background:rgba(255,255,255,.06);color:#fff}
    .staff-nav .icon{width:20px;height:20px;display:inline-flex;align-items:center;justify-content:center}
    .staff-nav .icon svg{width:20px;height:20px;stroke:currentColor;fill:none}
    .staff-sep{height:2px;background:linear-gradient(90deg,#ef4444,#f59e0b,#ef4444);border-radius:2px;opacity:.95;margin:6px 6px 2px;background-size:200% 100%;animation:sepSlide 6s linear infinite}
    @keyframes sepSlide{0%{background-position:0% 50%}100%{background-position:100% 50%}}
    .staff-main{flex:1;background:#f5f5f0;width:calc(100% - 240px);padding:0;margin:0;margin-left:240px;overflow-y:auto;overflow-x:hidden;min-height:100vh}
    .staff-top{display:flex;align-items:center;justify-content:space-between;padding:12px 16px;border-bottom:1px solid #1f3a50;background:#fff}
    .staff-content{padding:18px}
    .btn{display:inline-flex;align-items:center;gap:8px;background:#0f6bdc;color:#fff;border:none;border-radius:10px;padding:8px 12px;font-weight:700;text-decoration:none}
    .btn.plain{background:transparent;color:#cbd5e1;border:1px solid rgba(255,255,255,.2)}
    .btn.plain:hover{background:rgba(255,255,255,.06);color:#fff}
    
    /* Logout button styles */
    .logout-btn {
      margin-top: auto;
      color: #ef4444 !important;
      border-top: 1px solid rgba(255, 255, 255, 0.1);
      margin-top: 20px;
      padding-top: 12px;
    }
    .logout-btn:hover {
      background: rgba(239, 68, 68, 0.1) !important;
    }
    .logout-btn .icon {
      color: #ef4444 !important;
    }
    /* Hide scrollbar for sidebar */
    .staff-aside::-webkit-scrollbar {
      display: none; /* Chrome, Safari, Opera */
    }
    /* Hide scrollbar for main content */
    .staff-main::-webkit-scrollbar {
      display: none; /* Chrome, Safari, Opera */
    }
  </style>
{% endblock %}

{% block body %}
<style>
  /* Ensure staff layout takes full width */
  body { overflow-x: hidden; }
  .staff-shell { 
    display: flex; 
    min-height: 100vh; 
    width: 100%; 
    margin: 0;
    padding: 0;
  }
</style>
  <div class="staff-shell">
    <aside class="staff-aside">
      <div class="staff-brand" aria-hidden="true"></div>
      <div class="profile">
        <a href="{{ path('app_home') }}" class="avatar" aria-label="Go to homepage"><img src="{{ asset('image/logo.png') }}" alt="Logo"></a>
        <div class="meta">
          <a href="{{ path('app_home') }}" style="text-decoration: none;">
            <div class="status" id="staffStatus" style="cursor: pointer;">
              <span class="dot" aria-hidden="true"></span>
              <span id="staffStatusText" style="color: #fff;">Patrick's Cold Cuts</span>
            </div>
          </a>
        </div>
      </div>
      <div class="staff-sep" aria-hidden="true"></div>
      <nav class="staff-nav">
        <a href="{{ path('staff_dashboard') }}">
          <span class="icon" aria-hidden="true" style="color:#1e90ff">
            <svg viewBox="0 0 24 24" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M3 13h8V3H3v10zM13 21h8v-8h-8v8zM13 3h8v6h-8V3zM3 21h8v-6H3v6z"/>
            </svg>
          </span>
          <span>Dashboard</span>
        </a>
        <a href="{{ path('staff_product_index') }}">
          <span class="icon" aria-hidden="true" style="color:#ec4899">
            <svg viewBox="0 0 24 24" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M21 16V8a2 2 0 0 0-1-1.73l-7-4a2 2 0 0 0-2 0l-7 4A2 2 0 0 0 3 8v8a2 2 0 0 0 1 1.73l7 4a2 2 0 0 0 2 0l7-4A2 2 0 0 0 21 16z"/>
              <path d="M3.3 7L12 12l8.7-5"/>
            </svg>
          </span>
          <span>Products</span>
        </a>
        <a href="{{ path('staff_category_index') }}">
          <span class="icon" aria-hidden="true" style="color:#0ea5e9">
            <svg viewBox="0 0 24 24" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M3 6h13"/>
              <path d="M3 12h13"/>
              <path d="M3 18h13"/>
              <path d="M19 6h2"/>
              <path d="M19 12h2"/>
              <path d="M19 18h2"/>
            </svg>
          </span>
          <span>Categories</span>
        </a>
        <a href="{{ path('staff_order_index') }}">
          <span class="icon" aria-hidden="true" style="color:#8b5cf6">
            <svg viewBox="0 0 24 24" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M6 2L3 6v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2V6l-3-4z"/>
              <line x1="3" y1="6" x2="21" y2="6"/>
              <path d="M16 10a4 4 0 0 1-8 0"/>
            </svg>
          </span>
          <span>Orders</span>
        </a>
        <a href="{{ path('staff_profile') }}" title="Profile">
          <span class="icon" aria-hidden="true" style="color:#8b5cf6">
            <svg viewBox="0 0 24 24" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"/>
              <circle cx="12" cy="7" r="4"/>
            </svg>
          </span>
          <span>My Profile</span>
        </a>
      </nav>
      
      <!-- Logout Button -->
      <div class="staff-nav" style="margin-top: auto; padding-top: 16px; border-top: 1px solid rgba(255, 255, 255, 0.1);">
        <a href="{{ path('app_logout') }}" class="logout-btn">
          <span class="icon" aria-hidden="true">
            <svg viewBox="0 0 24 24" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M9 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h4"/>
              <polyline points="16 17 21 12 16 7"/>
              <line x1="21" y1="12" x2="9" y2="12"/>
            </svg>
          </span>
          <span>Logout</span>
        </a>
      </div>
    </aside>
    <main class="staff-main" style="flex: 1; padding: 0; overflow-y: auto; overflow-x: hidden;">
      <div style="width: 100%; height: 100%;">
        {% block staff_content %}{% endblock %}
      </div>
    </main>
  </div>
{% endblock %}

{% block javascripts %}
  {{ parent() }}
  <script>
    document.addEventListener('DOMContentLoaded', function() {
      // Function to fix layout issues
      function fixLayout() {
        // Force a reflow to ensure all elements are properly sized
        document.body.style.display = 'none';
        document.body.offsetHeight; // Trigger reflow
        document.body.style.display = '';
        
        // Ensure staff content takes full width
        const staffMain = document.querySelector('.staff-main');
        if (staffMain) {
          staffMain.style.flex = '1';
          staffMain.style.width = 'calc(100% - 240px)';
          staffMain.style.marginLeft = '240px';
        }
      }

      // Run once immediately
      fixLayout();
      
      // Run again after a short delay to catch any dynamic content
      setTimeout(fixLayout, 100);
      
      // Run one more time after all resources are loaded
      window.addEventListener('load', fixLayout);
      
      // Fix for WebKit browsers
      if (navigator.userAgent.indexOf('Safari') !== -1 || navigator.userAgent.indexOf('Chrome') !== -1) {
        window.addEventListener('pageshow', function(event) {
          if (event.persisted) {
            fixLayout();
          }
        });
      }
    });
  </script>
{% endblock %}

