# CSRF protection configuration
framework:
    form:
        csrf_protection:
            enabled: true

    csrf_protection:
        # Use session-based tokens for forms (more reliable)
        # Stateless tokens can cause issues with form resubmission
        enabled: true
